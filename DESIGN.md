# DESIGN.md — WeDesign

Визуальная система проекта. Читается перед любой правкой вёрстки.

## Чего в этом проекте не бывает

Список составлен по типовым провалам ИИ-вёрстки. Каждый пункт — запрет, а не пожелание.

- **Раздутый первый экран.** Заголовок не больше 56px на десктопе. Не 96, не 120.
- **Дежурные градиенты.** Никаких mesh-пятен, парящих шаров и радужных заливок
  под текстом. Исключение одно: фон первого экрана — небо с холмами. Это не
  украшение, а сцена, на которой стоит карточка ввода. Внутри контента
  градиентов нет.
- **Мягкие карточки везде.** Тень — не способ отделить блок. Разделяет линия или фон. Тень только у того, что действительно поднято над страницей: модальное окно, выпадающий список.
- **Произвольные отступы.** Только значения из шкалы ниже. `margin-top:18px` — ошибка, даже если «так лучше смотрится».
- **Одинаковый вес у всего.** На экране должен быть один главный элемент. Если всё крупное и контрастное — не выделяется ничего.
- **Скругления «побольше».** Радиусы только из шкалы. 24px на карточке размером с визитку — это не мягко, это шаблонно.
- **Декор без работы.** Иконка-заглушка, цветной квадратик «для красоты», разделитель ради ритма — убираются.

## Шкала отступов

Кратно 4. Других значений нет.

| Токен | Значение | Где |
|---|---|---|
| `--s1` | 4px | внутри строки |
| `--s2` | 8px | между иконкой и текстом |
| `--s3` | 12px | между полем и подписью |
| `--s4` | 16px | между карточками |
| `--s5` | 24px | внутренние поля карточки |
| `--s6` | 32px | между группами |
| `--s7` | 48px | заголовок и содержимое секции |
| `--s8` | 64px | вертикальный шаг секции на телефоне |
| `--s9` | 96px | вертикальный шаг секции на десктопе |

## Типографика

Один шрифт на весь интерфейс: **Golos Text**. Моноширинный **JetBrains Mono** — только для кодов заказов, сумм и служебных меток.

| Роль | Размер | Насыщенность | Межбуквенное |
|---|---|---|---|
| Заголовок страницы | 40 / 56px | 600 | −0.02em |
| Заголовок секции | 28 / 34px | 600 | −0.02em |
| Заголовок блока | 19px | 600 | −0.01em |
| Основной текст | 16px | 400 | 0 |
| Вводный абзац | 18px | 400 | −0.01em |
| Мелкий | 14px | 400 | 0 |
| Служебный | 12.5px | 500 | 0.02em |

Межстрочное: заголовки 1.1, текст 1.6.
Строка не длиннее 68 знаков.

## Цвет

| Токен | Значение | Роль |
|---|---|---|
| `--bg` | `#FFFFFF` | фон страницы |
| `--bg-alt` | `#F6F6F4` | фон чередующейся секции |
| `--ink` | `#16161A` | основной текст |
| `--ink-2` | `#5C5C66` | второстепенный текст |
| `--ink-3` | `#8A8A94` | подписи |
| `--line` | `#E4E4E1` | границы и разделители |
| `--ac` | `#1F4FD8` | действие, ссылки |
| `--ok` | `#1B7A4B` | подтверждение |
| `--warn` | `#9A6300` | внимание |
| `--err` | `#B3261E` | ошибка |

Акцент — один. Он обозначает действие, а не украшает. Если синий появился там, где нельзя кликнуть, это ошибка.

## Границы, радиусы, тени

- Граница: `1px solid var(--line)`. Толще — только у выделенного тарифа.
- Радиусы: `8px` поля и кнопки, `12px` карточки, `999px` только для меток-таблеток.
- Тень одна на весь проект: `0 8px 24px rgba(22,22,26,.08)`. Применяется только к
  тому, что действительно поднято над страницей: окно согласия и карточка ввода
  в первом экране. Больше нигде.

## Компоненты

**Кнопка.** Высота 44px, поля 20px, радиус 8px. Основная — заливка акцентом, белый текст. Вторичная — прозрачная с границей. Третья по важности — просто текст со ссылкой. Больше двух кнопок рядом не ставится.

**Поле ввода.** Высота 44px, граница `--line`, при фокусе — граница акцентом, без свечения. Подпись 12.5px над полем. Ошибка — красная граница и текст под полем.

**Карточка.** Белый фон, граница 1px, радиус 12px, внутренние поля 24px. Без тени. При наведении меняется цвет границы, положение не меняется.

**Секция.** Вертикальный шаг 96px на десктопе, 64px на телефоне. Заголовок и содержимое разделены 48px. Чередование фона — только `--bg` и `--bg-alt`.

## Иерархия

На каждом экране ровно один элемент первого уровня. Порядок внимания задаётся размером и цветом, а не рамками и тенями.

Проверка: сощурьтесь и посмотрите на экран. Видно одно пятно — верно. Видно пять одинаковых — переделать.

## Что делает вёрстку рукотворной

Правила выше убирают признаки машины. Эти — добавляют признаки человека.

**Не всё по центру.** Центральное выравнивание — один вариант из четырёх, и не
самый частый. Заголовок слева, текст в колонке справа, блок со сдвигом от края
читаются как решение, а не как настройка по умолчанию.

**Края шкалы, а не середина.** Подписи 11px с разрядкой 0.22em, заголовки
крупные, среднего размера почти нет. Машина держит всё в диапазоне 16–24px —
поэтому ничего и не выделяется.

**Раскладка следует за содержимым.** Две услуги не раскладываются в сетку на
три колонки. Шесть не превращаются в стену карточек. Короткие ответы не
прячутся под раскрывашку — это лишний клик ради вида.

**Приёмы из печати.** Цифра, вынесенная на поле. Текст, заходящий на цветную
плашку. Тонкая линейка с номером. Висячая пунктуация у цитаты. Цифры одной
ширины в столбце цен.

**Текстура.** Едва заметное зерно поверх страницы — 3.5% на светлых палитрах,
5.5% на тёмных, с разным режимом наложения. Плоская заливка выглядит как
экспорт из редактора.

## Проверка перед сдачей

1. Все отступы из шкалы?
2. Размеры шрифта из таблицы?
3. Акцентный цвет только на кликабельном?
4. Теней нет нигде, кроме окна согласия и карточки ввода?
5. Один главный элемент на экране?
6. Ни одного декоративного элемента без работы?
7. Раскладка подходит количеству содержимого, а не наоборот?
8. Есть ли на странице хоть один приём, которого не даёт настройка по умолчанию?
